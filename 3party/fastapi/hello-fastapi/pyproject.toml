[project]
name = "hello-fastapi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.135.3",
    "pydantic>=2.12.5",
]

[dependency-groups]
dev = [
    "ipython>=9.12.0",
    "ruff>=0.15.8",
]
